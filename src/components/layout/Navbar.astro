---
import ThemeToggle from "./ThemeToggle.jsx";
import MobileMenu from "./MobileMenu.jsx";

const pathname = Astro.url.pathname;
const links = [
  { href: "/", label: "Home" },
  { href: "/projects", label: "Projects" },
  { href: "/about", label: "About" },
  { href: "/contact", label: "Contact" },
];
---

<header class="sticky top-0 z-40 border-b border-border/70 bg-bg-elevated/82 backdrop-blur-xl">
  <div class="mx-auto flex max-w-7xl items-center justify-between gap-6 px-4 py-4 sm:px-6 lg:px-8">
    <a class="group inline-flex items-center gap-3" href="/">
      <span class="inline-flex h-11 w-11 items-center justify-center rounded-2xl border border-primary/30 bg-primary-soft font-display text-lg font-bold text-primary shadow-[0_0_0_1px_rgba(34,197,94,0.12)]">
        TH
      </span>
      <span>
        <span class="block font-display text-base font-bold tracking-[-0.03em] text-text">
          Thein Htoo Aung
        </span>
        <span class="block text-sm text-muted">Odoo and system builder</span>
      </span>
    </a>

    <nav class="hidden items-center gap-2 rounded-full border border-border bg-card/65 p-1.5 shadow-soft lg:flex">
      {links.map((link) => {
        const active =
          link.href === "/" ? pathname === "/" : pathname.startsWith(link.href);
        return (
          <a
            class:list={[
              "relative rounded-full px-4 py-2.5 text-sm font-medium",
              active
                ? "bg-primary-soft text-primary"
                : "text-muted hover:text-text",
            ]}
            href={link.href}
          >
            {link.label}
            <span
              class:list={[
                "absolute inset-x-4 bottom-1 h-px bg-secondary transition-opacity",
                active ? "opacity-100" : "opacity-0 hover:opacity-100",
              ]}
            />
          </a>
        );
      })}
    </nav>

    <div class="hidden items-center gap-3 lg:flex">
      <ThemeToggle client:load />
    </div>

    <MobileMenu client:load links={links} pathname={pathname} />
  </div>
</header>
